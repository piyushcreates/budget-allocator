import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.budgetallocator',
  appName: 'Budget Allocator',
  webDir: 'dist'
};

export default config;
